#!/usr/bin/python3
def replace_in_list(my_list, idx, element):
    if idx < my_list:
        return original list
    elif idx >= my_list:
        return original list
    return my_list[idx] = element
